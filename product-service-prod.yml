spring:
  application:
    name: product-service

server:
  port: 8090

order:
  service:
    url: http://order-service:8085

eureka:
  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka